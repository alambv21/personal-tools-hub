# Personal Tools Hub is a 100% static, client-side application.
# It does not call any server-side API and requires NO environment
# variables or secret keys to build, run, or deploy.
#
# This file is kept only so .gitignore's "!.env.example" rule has
# something to allow; it intentionally has nothing to configure.
